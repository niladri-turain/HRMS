import 'package:flutter/material.dart';

class ClientVisitScreen extends StatelessWidget {
  const ClientVisitScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Clients Visit'),
      ),
    );
  }
}
